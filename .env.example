# These keys are now used on the server side (in /api/ai-proxy.js)
# Do NOT prefix them with VITE_ if you want to keep them secret.
YANDEX_API_KEY=your_api_key_here
YANDEX_FOLDER_ID=your_folder_id_here
